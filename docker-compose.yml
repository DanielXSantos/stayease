version: '3'

services:
  postgres:
    image: postgres:latest
    container_name: stayeasy
    environment:
      POSTGRES_DB: stayeasy
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
